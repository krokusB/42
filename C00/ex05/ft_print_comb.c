#include <unistd.h>

void ft_print_comb(void);

int main(void)
    {
        ft_print_comb();#
        return(0);
    }
//all different combinations of three different digits
//is es so klar dass es immer eins mehr ist?
//wie ist die Ausschlußformel-kriterium?
//oder ist das automatisch, wenn ich die nächste Zahl immer eins höher starten lasse?
void ft_print_comb(void)
    {
        
    }
